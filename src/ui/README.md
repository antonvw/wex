# ui library

This library contains ui classes and functions for wex.
It uses the common and predecessor wex libraries.

The lsp client and server handling is done in the lsp library,
that one is not used here, instead ui presentations are received
here using events, and only use classes from lsp.h.
