component_depth: 2

languages:
- name: cpp

  production:
    exclude:
      - /ci/.*
      - /data/.*
      - /external/.*
      - /locale/.*
      - /src/lexers/.*
      - /test/.*
      
  test:
    include:
    - /test/.*
