component_depth: 2

languages:
- name: cpp

  production:
    exclude:
      - /ci/.*
      - /data/.*
      - /external/.*
      - /locale/.*
      - /sample/.*
      - /src/syntax/.*
      - /test/.*
      
  test:
    include:
    - /test/.*
