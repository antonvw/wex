version: 3.1.0.{build}

branches:
  only:
    - master

clone_folder: c:\projects\wxex

platform:
  - x64

configuration:
  - Release

install:
  - cmd: cinst -y wxwidgets
  
before_build:
  - mkdir build
  - ps: cd build
  - cmake ..

test: OFF

artifacts:
- path: 'syncped\syncped.exe'
